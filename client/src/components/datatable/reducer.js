const initialState = {};

const datatable = (state = initialState, action) => {
  switch (action.type) {
    case 't':
      return state;

    default:
      return state;
  }
};

export default datatable;
